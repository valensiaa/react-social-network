let initialState = {
    friendsSidebar: [
        {id: 1, name: 'Basya'},
        {id: 3, name: 'Valya'},
        {id: 2, name: 'Andrii'}
      ]
}

const sidebarReducer = (state = initialState, action) => {

    return state
}

export default sidebarReducer